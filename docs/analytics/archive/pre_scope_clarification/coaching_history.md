# Local workflow continuity

- Latest preference: a simple experiment-loading workflow and a minimal notebook. Use `load_seasons` for a population, `load_season` for one publication and `inspect_season` for tables/columns. Avoid validator internals, diagnostic reports and lengthy checkpoint teaching. No learner approval or further input was required for this authorized batch.
- Ownership: the primary owns library implementation; the background task owns tests/notebook/docs and this real import. Call back only for actual failed tests or real import assertions. Archive originals before edits, preserve unrelated changes and running D-Tale, and never alter collected source data.
- Selected population: **all available leagues for `['22_23', '23_24', '24_25']`** from `data/xDiyo_data`. The 16 September 2026 import loaded **39 publications across 13 leagues**, with **13,976 matches**, **3,303,574 statistics rows** and **27,072 pregame rows**, separately. No league-season combinations are missing among these 13 leagues. Counts describe collected cohorts, not independently verified complete provider fixtures.
- Practical API: `load_seasons` discovers top-level publications in supplied season order, then league order. Exact league filters are optional. `.tables`, properties and brackets expose separate frames; every row gains Arrow string `source_league` and `source_season`. Original values/order/nulls remain. Optional source columns stay missing where absent; missing combinations are not fabricated. Shots remain independently selectable.
- Saved selections: **39 version-1 records** are in `experiment/initial_population/selections`, outside the source. Records pin each publication version, not discovery membership; newly available matching publications can expand a later call. Provenance and the evidence JSON record the actual population. Earlier successful records can remain when a later partition fails. Whole-file hash checking remains opt-in.
- Current evidence: **57 tests passed**, comprising the unchanged 36 slim cases and 21 new cases. All **117 loaded table partitions** matched direct source Parquet exactly; all **195 selected source-file fingerprints** remained unchanged. The **9-cell notebook (6 code cells)** executed top-to-bottom in a fresh `misc314_py314` kernel and displays league-by-season counts and small samples. No actual test/import assertion failures, callbacks or implementation edits by this task. The usual nonfatal Windows/ZMQ selector warning did not prevent execution.
- Coverage policy: coverage is optional and not requested for this experiment. Synthetic tests cover exports that never declared it, and a new version omitting both its descriptor and file while preserving the old version. Both loaders and the inspector work; explicit absent-coverage requests raise `KeyError`. Do not remove files behind unchanged manifests or rewrite collected versions.
- Recovery: `docs/analytics/archive/pre_multiseason_experiment/` preserves the previous notebook and six affected guides/notes with verified hashes. `pre_simplification/` still contains the original 123-cell notebook, source displays, original tests and older guides/tracker/history. No files were removed; prior evidence JSONs remain unchanged. Latest evidence: `docs/analytics/multiseason_import_check.json`.
- Checked source: `loading.py` SHA256 `b77d29fbe2b09c4036475beb2138b779d8fcddb712e1c9222082c8497b784bbf`; all five package-source hashes remained stable through the suite, import and notebook execution and are recorded in the evidence.
- Next context: use `.misc314`, preserving the scraper environment. All 18 table descriptions remain in `docs/analytics/season_inspection.md`; `pregame.position` is match-specific team position, not a complete league table. Population selection and import are complete. Feature inputs, timing/history policies, joins and model fitting remain subsequent work.
