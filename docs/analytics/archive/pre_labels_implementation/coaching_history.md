# Local workflow continuity

- Latest preference: a simple loading/selection/history/feature workflow and short practical notebooks. Use `load_seasons`, `load_season`, `inspect_season`, `list_stat_bundles`, `select_stats`, `build_team_history` and `evaluate_features`. Avoid validator internals, diagnostic reports and lengthy checkpoint teaching. No learner approval or further input was required for this authorized batch.
- Ownership: the primary owns library implementation; the background task owns tests/notebook/docs and this real import. Call back only for actual failed tests or real import assertions. Archive originals before edits, preserve unrelated changes and running D-Tale, and never alter collected source data.
- Selected population: **all available leagues for `['22_23', '23_24', '24_25']`** from `data/xDiyo_data`. The 16 September 2026 import loaded **39 publications across 13 leagues**, with **13,976 matches**, **3,303,574 statistics rows** and **27,072 pregame rows**, separately. No league-season combinations are missing among these 13 leagues. Counts describe collected cohorts, not independently verified complete provider fixtures.
- Practical API: `load_seasons` discovers top-level publications in supplied season order, then league order. Exact league filters are optional. `.tables`, properties and brackets expose separate frames; every row gains Arrow string `source_league` and `source_season`. Original values/order/nulls remain. Optional source columns stay missing where absent; missing combinations are not fabricated. Shots remain independently selectable.
- Saved selections: **39 version-1 records** are in `experiment/initial_population/selections`, outside the source. Records pin each publication version, not discovery membership; newly available matching publications can expand a later call. Provenance and the evidence JSON record the actual population. Earlier successful records can remain when a later partition fails. Whole-file hash checking remains opt-in.
- Statistic selection: attack/defense use editable period-independent `(group_name, key)` pairs. `*_all` includes every supplied period; totals/halves filter `ALL`/`1ST`/`2ND` from that selection. Totals are never sums of halves. `all_stats` includes uncategorized/new measures. Bundles and exact triples form a union without collapsing groups or repeating selected rows. Per-call categories replace named defaults or add categories. Defense means observed defensive measures, not derived opponent statistics. Standings preserve pregame positions separately; matches pass through and shots stay separate.
- Team history: import `build_team_history` from `xdiyo_analytics.histories`. It returns two observed rows per match, with reversed team/opponent identities, values and positions. Statistic columns retain period/group/key/field identities; `attrs['stat_columns']` maps escaped names back to labels. Full UTC kickoff time is retained. Goals use provider current scores, and W/D/L requires finished status plus both scores; penalty winners are not inferred. Missing times/observations retain rows. No lag or historical-eligibility assumption is built in.
- Earlier history evidence: **109 tests passed**, including **26 new history cases** and all prior 83 cases. The focused 26-case suite also passed. The real history has **27,952 rows × 83 columns**, with **62 team/opponent statistic columns**. Every context/result row was independently checked; Premier League 2024/25 additionally matched **47,120 statistic cells** and **1,520 position cells** against direct source Parquet. The **12-cell notebook (9 code cells)** executed in a fresh `misc314_py314` kernel. All **195 source files**, **39 saved records** and the experiment population remained unchanged.
- Resolved findings: the primary fixed overflow for unrepresentable Arrow timestamp seconds; ±1e30 and ±infinity now become NaT in regression tests. Two later assertions were relaxed because invalid mixed signed/unsigned team IDs already raised ValueError with different wording. Two failure-only callbacks were sent; no success callback or worker implementation edit occurred. A pandas all-NA concatenation FutureWarning and the familiar notebook ZMQ warning were nonfatal.
- Recovery: `docs/analytics/archive/pre_team_history/` preserves the preceding notebook and five affected guides/notes. All earlier archives remain intact, including the original 123-cell notebook. No files were removed; prior evidence JSONs remain unchanged. Current evidence: `docs/analytics/team_history_check.json`; usage/columns: `docs/analytics/team_history.md`.
- Checked source: `histories/team.py` SHA256 `0f346230b8bd172d41a2ecf84f7eb394f839f49915ef84cafc30587181e2209c`; all eight package-source hashes were stable through the final suite and real-history/notebook checks. The primary's fix supersedes the initial `6f95bad212bcfa28377f8b84668a0c4225f5a9f7d7460e5d2f256e55b075d7a7` version.
- Latest feature contract: named `Stat`, `ForAgainst`, `H2H`, current `IsHome`/`NormalizedStanding`, lags, means/stds/Z-scores and optional EMA are implemented and verified. `Stat(None)` expands supplied periods separately. Default groups combine venues and cross seasons within team/competition; H2H adds the ordered opponent. Windows count eligible matches including missing-valued matches; reductions use finite values within the window, lags never skip them. Default min_periods is 1 and std ddof is 1 with n > ddof. Z-score includes the latest observation in its baseline; zero spread or a missing latest value yields missing. EMA uses alpha=2/(span+1), first-observed initialization and skips missing values without decay; it is optional and introduces no pilot prior/warm-up.
- Standing choice: compute distinct full-population competition/season team counts before splits/filtering. Normalize first place to 1 and last to 0; missing/invalid position or denominator defaults to **zero**, superseding earlier proposals for this feature only. No missing-season proxy is implemented.
- Timing choice: `cutoffs=None` is accepted and preferred when no earlier issuance time is requested. Availability timestamps are fully optional. Finished candidates require kickoff strictly before cutoff; explicit availability, if supplied, must be at or before cutoff. Default earlier finished kickoff is a retrospective proxy, not proof of completion/publication time. Target identity and boundary ties are excluded. Nested children use each historical row's cutoff. Context is not reconstructed as of an earlier cutoff. Exact aligned-Series, named-column and frozen-round examples are in `docs/analytics/feature_cutoffs.md`.
- Earlier feature verification: **141 tests passed**, including **32 new feature cases** and the 109 prior cases. Independent real-data calculations matched **760 rows × 22 feature columns** under default and two-day-earlier cutoffs; the named-column form matched the Series. Direct Parquet lookup matched **4,560 corner cells**; full-population team count was 20. The real two-day shift leaves these features unchanged; synthetic cases verify actual eligibility changes. The new **8-cell feature notebook (5 code cells)** executed in a fresh `misc314_py314` kernel. The existing loading notebook remains byte-for-byte unchanged.
- Feature recovery and preservation: `docs/analytics/archive/pre_features/` holds the five affected existing guides/notes; prior archives and 12 old evidence JSONs remain intact. All 12 package sources were stable across tests/real data/notebook, eight earlier test files remain unchanged, and all 195 source fingerprints plus 39 saved records were preserved. No library implementation edit, deletion or callback occurred in this batch. The existing pandas and notebook ZMQ warnings were nonfatal. Evidence: `docs/analytics/features_check.json`; guide: `docs/analytics/features.md`.
- Current ratings: `ratings.Glicko2` now adapts the unchanged `utils.glicko_rating` single numerical engine, inheriting tau=1.0 and epsilon=1e-6. Snapshot public `rating`/`rd` differ from internal `mu`/`phi`; the legacy object's mu/phi are public-scale names. Explicit `advance_periods` preserves legacy idle behavior, but replay/lookup do not insert idle periods. Equal-release events update against shared prior states. Scope defaults to competition across seasons; scope=() crosses competitions and adding season_id resets independently.
- Rating features and reuse: `MatchResultGlicko`, `StatGlicko(Stat(...))` and `Rating(name)` export both perspectives into ordinary training/reporting X. Statistic greater/equal/less maps to win/draw/loss, optionally reversed for lower-is-better; missing pairs skip the update. Periods and result/statistic definitions retain independent streams. `build_ratings` and `RatingRun.save/load` retain all numeric state fields independently of model selection. Numeric custom engines and external vector states are verified; graph/GAT training and incremental append remain future work. Availability is optional and proxy limitations remain explicit.
- Current verification: **37 focused rating tests and 178 full analytics tests pass**. Permanent legacy parity covers 36 periods and 288 idle comparisons with custom centers. Independent PL raw-source replay verified 760 snapshots for each of result/corners, 38,000 lookup cells and exact saved-run feature parity; the generated matrix is 760×13. Tight-solver reference agreement is about 1e-11; default epsilon1e-6 differences stay below 6.41e-6 public units and are recorded separately from exact legacy parity. An isolated wheel build/import succeeded with no installs. The separate **8-cell ratings notebook (5 code cells)** executed in a fresh kernel; both earlier notebooks are unchanged.
- Resolved rating findings: the primary corrected cumulative latest-kickoff provenance for delayed releases, including opponent-prior contributions. A numerical fixture was aligned with the confirmed tau=1 default; a real-reference assertion was adjusted only after tighter-tolerance replay established solver precision as its cause. Two failure callbacks were delivered after direct approvals required by automatic review; no success callback was sent. No worker library edits occurred. Initial failures/precision diagnosis remain in `.pytest_tmp/ratings_verification/`.
- Rating preservation: all 17 package sources are stable across final tests/real checks/wheel/notebook; utils/glicko_rating.py is unchanged. All 195 source fingerprints, 39 saved records,nine earlier test files and 13 prior evidence JSONs remain intact. `docs/analytics/archive/pre_ratings/` preserves five affected files plus the primary's later tracker version. Saved runs are under `experiment/ratings_demo/Premier_League_24_25/6d950fa327de4fc29c796c7cca101606/`. Guide/evidence: `docs/analytics/ratings.md`, `docs/analytics/ratings_check.json`; primary comparison: `docs/analytics/glicko_engine_comparison.md`.
- Next priority supersedes selecting the pilot recipe: establish the remaining reusable orchestration layers before designing/configuring the corner experiment. Start with aligned X/y/metadata and a declared prediction unit/layout (match or team-match), including match/team/time/group identities. No pilot feature recipe, model tuning or fitting is selected now.
- Reusable labels: statistic-derived quantities, perspective W/D/L +1/0/-1, configurable binary outcomes and a BetOption family for outcome/threshold experiments. Keep draw/push/void/missing distinct and mappings explicit. Support future parlay composition in the design; bookmaker-specific settlement rules remain unspecified.
- Execution/reporting: connect prepared persistent states, features/labels, assembly, temporal splits, training-only fitted transforms/model adapters and both reporter stages. Reuse prepared rating states across reports/models/folds. Pre-training reporting must use the declared layout and respect paired/ranking groups; post-training reporting also takes predictions and applicable model outputs.
- Continuity: use `.misc314`, preserving the scraper environment. Existing component notebooks, source selections and the eventual pilot remain references while orchestration is completed. All 18 table descriptions remain in `docs/analytics/season_inspection.md`; loading, selection, histories, features and rating integration remain verified. The latest delegated batch now verifies the authorized league/transition implementation and its documentation; source ownership remains with the primary task.

- Latest completed batch: League/LOO, explicit WarmStart, all three handoffs, movement-context adapters and rating transitions are verified. Unwrapped defaults remain unchanged. The full suite passes 275 tests, including 97 new cases. Top/bottom ranked priors are rating-only; custom engines need custom transition adapters and full state transitions once before field selection.
- Bounded real evidence: two pinned Premier League seasons, 760 matches / 1,520 rows; 3,040 raw cells, 24,320 league/LOO cells and 12,160 warm-up cells independently reconciled. Seventeen retained-team entries checked; new origin-unknown teams stay unknown. The saved transitioned run has 1,560 snapshots. Earlier-finished kickoff remains a retrospective availability proxy.
- Documentation and preservation: all 18 Python examples in the practical/API guides executed, the separate 9-cell notebook (5 code cells) ran fresh, and a 67-file wheel copy imported without installs. Existing three notebooks, old tests/evidence and source exports remain unchanged. Guides: docs/analytics/league_warmup.md and league_warmup_reference.md; complete matrix: league_warmup_documentation_checklist.md; evidence: league_warmup_check.json. No failure or success callbacks were needed.
