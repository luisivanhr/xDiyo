# Local workflow continuity

- Latest preference: the user wants a simple experiment-loading workflow and a minimal notebook. Use `inspect_season` and `load_season`; avoid validator internals, diagnostic reports and lengthy checkpoint teaching. No learner approval or further input was required for this authorized rewrite.
- Ownership: the primary owns library implementation; the background task owns tests/notebook/docs. Call back only for actual failed tests. Archive originals before replacing contents, preserve unrelated changes and running D-Tale, and never alter collected source data.
- Practical API: load a string or list/tuple of selected table names; access separate frames with `.matches/.statistics/.pregame/.shots` or `season['table']`. Statistics may load alone; cross-table checks run when matches are included. Default reads perform essential checks; whole-table hashes are opt-in. Existing version-1 selection records replay, and new records save after a successful whole load. No validation/status dictionaries remain.
- Current evidence: 36 compact tests passed. Normal and audited Premier League 2024/25 loads matched source Parquet exactly: matches 380 × 35, statistics 98,224 × 18, pregame 740 × 8. The active notebook has 9 cells, including 6 executed code cells, and ran top-to-bottom in `misc314_py314`. No actual test failures or callbacks; no implementation edits by this task.
- Recovery: `docs/analytics/archive/pre_simplification/` contains the original 123-cell notebook, source displays, four original test files and the prior guides/tracker/history, with checked hashes. No files were removed; prior evidence JSONs remain unchanged. Latest evidence: `docs/analytics/simplified_loading_check.json`.
- Checked source: `loading.py` SHA256 `e1682d89464d8a1c74159e14d75c457e8ec2f35b85b38d2ac5b6305e7987d693`; all five package-source hashes are recorded in the new evidence. A pre-test loading-file edit was captured by the immediate test baseline; results apply to that stable tested state.
- Context: use `data/xDiyo_data` and `.misc314`, preserving the scraper environment. All 18 table descriptions remain in `docs/analytics/season_inspection.md`; `pregame.position` is match-specific team position, not a complete league table. Shots remain separate. League selection, features and joins remain future decisions.
