# Local workflow continuity

- Latest preference: a simple experiment-loading/selection workflow and a minimal notebook. Use `load_seasons`, `load_season`, `inspect_season`, `list_stat_bundles` and `select_stats`. Avoid validator internals, diagnostic reports and lengthy checkpoint teaching. No learner approval or further input was required for this authorized batch.
- Ownership: the primary owns library implementation; the background task owns tests/notebook/docs and this real import. Call back only for actual failed tests or real import assertions. Archive originals before edits, preserve unrelated changes and running D-Tale, and never alter collected source data.
- Selected population: **all available leagues for `['22_23', '23_24', '24_25']`** from `data/xDiyo_data`. The 16 September 2026 import loaded **39 publications across 13 leagues**, with **13,976 matches**, **3,303,574 statistics rows** and **27,072 pregame rows**, separately. No league-season combinations are missing among these 13 leagues. Counts describe collected cohorts, not independently verified complete provider fixtures.
- Practical API: `load_seasons` discovers top-level publications in supplied season order, then league order. Exact league filters are optional. `.tables`, properties and brackets expose separate frames; every row gains Arrow string `source_league` and `source_season`. Original values/order/nulls remain. Optional source columns stay missing where absent; missing combinations are not fabricated. Shots remain independently selectable.
- Saved selections: **39 version-1 records** are in `experiment/initial_population/selections`, outside the source. Records pin each publication version, not discovery membership; newly available matching publications can expand a later call. Provenance and the evidence JSON record the actual population. Earlier successful records can remain when a later partition fails. Whole-file hash checking remains opt-in.
- Statistic selection: attack/defense use editable period-independent `(group_name, key)` pairs. `*_all` includes every supplied period; totals/halves filter `ALL`/`1ST`/`2ND` from that selection. Totals are never sums of halves. `all_stats` includes uncategorized/new measures. Bundles and exact triples form a union without collapsing groups or repeating selected rows. Per-call categories replace named defaults or add categories. Defense means observed defensive measures, not derived opponent statistics. Standings preserve pregame positions separately; matches pass through and shots stay separate.
- Current evidence: **83 tests passed**: the unchanged 55 loader/multi-season cases plus 28 selector cases. Independent selections on pinned Premier League 2024/25 verified all 12 category/period combinations and the example union: **22,762 statistic rows**, **740 standings rows**, **380 matches**. Input values, nulls, order, types and provenance were preserved. The **11-cell notebook (8 code cells)** executed in a fresh `misc314_py314` kernel and shows bundle discovery plus the requested union with exact possession statistics. It selected **746,880 statistic rows** over the unchanged 39-publication population. All **195 source-file fingerprints** and **39 saved records** remained unchanged. No actual failures, callbacks or implementation edits by this task.
- Recovery: `docs/analytics/archive/pre_stat_selection/` preserves the preceding notebook and five affected guides/notes. Earlier `pre_scope_clarification/`, `pre_multiseason_experiment/` and `pre_simplification/` archives remain intact, including the original 123-cell notebook. No files were removed; prior evidence JSONs remain unchanged. Current evidence: `docs/analytics/stat_selection_check.json`; practical definitions: `docs/analytics/stat_selection.md`.
- Checked source: `selection.py` SHA256 `386633052a3ecd6df68c85ba7f88c448b69392179d200528a47f544d2d3b3d16`; all six package-source hashes stayed stable through tests, representative selection and fresh notebook execution and are recorded in the evidence.
- Next context: use `.misc314`, preserving the scraper environment. All 18 table descriptions remain in `docs/analytics/season_inspection.md`; `pregame.position` is match-specific team position, not a complete league table. Population import and general statistic selection are verified. Actual feature inputs, timing/history policies, history construction, joins and model fitting remain subsequent work.
