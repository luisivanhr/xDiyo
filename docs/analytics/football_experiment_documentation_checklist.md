# FootballExperiment documentation and verification coverage

Verification status is recorded in [football_experiment_check.json](football_experiment_check.json).
This batch uses a preserved revision while the shared checkout proceeds to a
separate prediction/persistence increment. Earlier evidence remains tied to its
own source hashes.

| Contract | Evidence |
| --- | --- |
| Fixed real EstimatorAdapter run → exact reuse → explicit load | fixed tests; guide; notebook 14 |
| Data/order/definitions/folds/config/factory invalidate reuse | fixed and identity tests |
| Changed notebook constants invalidate; cell location does not | identity tests |
| Interrupted grid reuses only complete trials, then fresh holdout | search tests |
| Nested scopes and stateful callable source fingerprints | search tests plus preserved nested regressions |
| Native resume retains optimizer/RNG/preprocessing/cursor/history | checkpoint tests against uninterrupted numerical reference |
| Writer failure leaves prior checkpoint usable; pointer containment | checkpoint tests |
| Plain sklearn skip/raise; fresh namespaces; offered frequency | checkpoint tests |
| Refit learns feature selection/scaling on actual refit fitting rows | refit tests in both layouts |
| Nullable large IDs/labels, probability columns, StringDtype and old bundle compatibility | schema tests |
| Combined selection/pre/post display; final-only metrics and refreshed leaderboard | reporting/search tests; offline browser |
| Generated names and escaped expandable configurations | reporting tests; guide and browser |
| Legacy absent state is never invented; exact saved HTML | legacy tests |
| Public imports, all options, recovery protocols and helpers | reference signatures; isolated wheel |
| Executable documentation and fresh notebook | guide checks; new notebook only |
| Documentation layout and equations | local rendering and reviewed screenshots |
| Prior artifacts/notebooks/data/assets unchanged | final preservation audit |

The primary repaired duplicated nested candidate-source evaluation and recovery
schema loss after genuine failure callbacks. Original failed logs and frozen
sources are retained. Worker fixture corrections were Plotly dictionary-order
comparison and the Artifact.data attribute; these required no library changes.

Limits: all model fits are synthetic; no real-data pilot, package installation,
scheduled training or prior notebook execution. Fresh-kernel execution and saved
offline native iframe behavior are checked; live Jupyter frontend remains
unverified. Native resume is adapter-dependent and numerical result recovery
does not restore executable models. The next increment has separate evidence.
